# ------------------------
# Node / JavaScript
# ------------------------
# node_modules in any package (root, client, server)
node_modules/
**/node_modules/

# npm / yarn / pnpm logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# lockfile caches (do not ignore actual lockfiles like package-lock.json / yarn.lock / pnpm-lock.yaml)
.pnp.*
.yarn/*
!.yarn/releases
!.yarn/plugins
.yarnrc.yml
.pnpm-store/

# ------------------------
# Build / output
# ------------------------
# React (CRA / Vite)
dist/
build/
.vite/
.cache/

# Next / Nuxt / other frameworks
.next/
.nuxt/
out/

# coverage / test output
coverage/
.nyc_output/
jest-cache/

# TypeScript build info
*.tsbuildinfo

# general temporary / compiled files
tmp/
temp/
*.tmp
*.temp

# ------------------------
# Environment / secrets
# ------------------------
# Local env files (do not commit real secrets)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local
.env.test
.env.production
.env.docker

# key / cert files
*.pem
*.key
*.crt

# ------------------------
# Logs / runtime data
# ------------------------
logs
*.log
logs/*.log
pids/
*.pid
*.seed
*.pid.lock
npm-debug.log*
yarn-debug.log*

# ------------------------
# Database / local data
# ------------------------
# local MongoDB data dirs (if you use docker-compose or local mongo)
data/
data/db/
mongo/data/

# ------------------------
# Docker
# ------------------------
# local overrides & sensitive compose files
docker-compose.override.yml
docker-compose.*.yml
.Dockerfile*
docker-env.*

# ------------------------
# IDE / Editor
# ------------------------
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
.history/

# ------------------------
# OS files
# ------------------------
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# ------------------------
# Misc / Generated
# ------------------------
# macOS Finder attributes
Icon?
ehthumbs.db

# npm pack
*.tgz

# lock files we DO NOT recommend ignoring: package-lock.json, yarn.lock, pnpm-lock.yaml
# (keep them committed for reproducible installs)
